/**
 * תצורת מפה - Generated Image June 08, 2026
 * ניתן לשנות את הפרמטרים כאן כדי להתאים לתמונות שונות
 */

const mapConfig = {
    // נתיב לתמונה
    imagePath: 'מפה-חדשה-אבנים.jpg',
    
    // גודל התמונה (יותאם אוטומטית אחרי טעינה)
    imageWidth: 1920,
    imageHeight: 1440,
    
    // גודל viewport (המסך)
    viewportWidth: 480,
    viewportHeight: 320,
    
    // זיהוי צבעי שבילים (HSL)
    // H = Hue (גוון): 0-360
    // S = Saturation (רוויה): 0-100
    // L = Lightness (בהירות): 0-100
    // ⚠️ לחץ L במשחק כדי לראות מפת debug ולכוונן את הערכים
    pathColors: {
    hueRange: [30, 275],
    saturationRange: [20, 60],
    lightnessRange: [41, 75]
},
    
    // השלמת שבילים
    fillGapsSize: 15,      // מילוי חורים עד גודל 15 פיקסלים
    connectDistance: 30,   // חיבור שבילים במרחק עד 30 פיקסלים
    
    // שבילים ידניים (להוספה ידנית במקומות שהזיהוי האוטומטי החמיץ)
    // דוגמאות:
    manualPaths: [
        // { type: 'rect', x: 100, y: 200, w: 50, h: 200 },
        // { type: 'circle', x: 300, y: 400, radius: 50 }
    ],
    
    // גודל טייל לאופטימיזציה (פיקסלים)
    // ערך קטן יותר = דיוק גבוה יותר אבל ביצועים נמוכים יותר
    tileSize: 8,
    
    // מיקום התחלתי של השחקן (בפיקסלים)
    playerStartX: 960,  // מרכז התמונה
    playerStartY: 720
};

// ========================================
// הוראות שימוש:
// ========================================
// 
// 1. להחלפת תמונה:
//    - שים את התמונה החדשה בתיקיית הפרויקט
//    - עדכן את imagePath למעלה
//
// 2. התאמת זיהוי שבילים:
//    - הפעל את מצב ה-Debug (לחץ L במשחק)
//    - תראה אזורים ירוקים (מותר) ואדומים (אסור)
//    - שנה את pathColors עד שהשבילים מזוהים נכון
//
// 3. הוספת שבילים ידניים:
//    - אם יש אזורים שלא מזוהים, הוסף אותם ל-manualPaths
//    - מלבן: { type: 'rect', x: X, y: Y, w: רוחב, h: גובה }
//    - עיגול: { type: 'circle', x: X, y: Y, radius: רדיוס }
//
// 4. אופטימיזציה:
//    - אם המשחק איטי, הגדל את tileSize (למשל 10)
//    - אם הדיוק לא מספיק, הקטן את tileSize (למשל 3)
//
// ========================================
